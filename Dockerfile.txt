# Imagem base atualizada (projeto desenvolvido por Silas Cruz Ferreira Cesario)
FROM python:3.11-slim

# Diretório principal do aplicativo
WORKDIR /app

# Evita buffering de saída em logs
ENV PYTHONUNBUFFERED=1

# Instala as dependências do projeto
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copia o restante dos arquivos para dentro do container
COPY . .

# Porta utilizada pelo Streamlit
EXPOSE 8501

# Comando para iniciar o dashboard
CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
